[theme]
base = "dark"
backgroundColor    = "#0D1B2A"
secondaryBackgroundColor = "#162032"
primaryColor       = "#1A6BFF"
textColor          = "#E0E8FF"

# Nota: porta configurada apenas para desenvolvimento local via run.bat
# Streamlit Cloud gerencia a porta automaticamente (8501)
