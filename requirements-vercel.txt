# Dependências para deploy Vercel (sem Streamlit/plotly/pandas)
# requirements.txt continua servindo o Streamlit Cloud

# ── Servidor ──────────────────────────────────────────────────
fastapi==0.111.*
uvicorn[standard]==0.30.*
python-multipart==0.0.9

# ── IA — Google AI Studio (Gemini API gratuita) ────────────────
google-generativeai==0.7.*

# ── Extração de arquivos ───────────────────────────────────────
pdfplumber==0.11.*
python-docx==1.1.*
openpyxl==3.1.*

# ── Validação e modelos ────────────────────────────────────────
pydantic==2.7.*
pydantic[email]==2.7.*

# ── Configuração ───────────────────────────────────────────────
python-dotenv==1.0.*

# ── Autenticação (JWT + bcrypt) ────────────────────────────────
python-jose[cryptography]==3.3.*
passlib[bcrypt]==1.7.*
bcrypt==3.2.2

# ── Banco de dados PostgreSQL (Supabase) ──────────────────────
psycopg2-binary==2.9.*

# ── Adaptador Vercel serverless (ASGI → Lambda) ───────────────
mangum==0.19.*
