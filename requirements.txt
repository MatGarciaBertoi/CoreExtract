# ── Servidor ──────────────────────────────────────────────────
fastapi==0.111.*
uvicorn[standard]==0.30.*
python-multipart==0.0.9

# ── IA — Google AI Studio (Gemini API gratuita) ────────────────
google-generativeai==0.7.*

# ── Extração de arquivos ───────────────────────────────────────
pdfplumber==0.11.*
python-docx==1.1.*
openpyxl==3.1.*
pandas==2.2.*

# ── Validação e modelos ────────────────────────────────────────
pydantic==2.7.*
pydantic[email]==2.7.*

# ── Configuração local ─────────────────────────────────────────
python-dotenv==1.0.*

# ── Autenticação (JWT + bcrypt) ────────────────────────────────
python-jose[cryptography]==3.3.*
passlib[bcrypt]==1.7.*

# ── Dashboard (local only — em produção o dashboard roda no Streamlit Cloud) ──
# Descomente abaixo apenas para rodar o dashboard localmente:
# streamlit>=1.44,<2.0
# plotly>=5.22,<6.0
